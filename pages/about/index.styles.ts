import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const DivAboutStyled = styled.div`

`

export const AboutStyled = styled(Box) `
    height: 100vh;
    .container {
        height:100vh;
    }
`